def mul(a,b):
    return a*b

#lambda
l_mul = lambda a,b: a*b  #we can write code or function in line but for multiple functions we can,t use
print(l_mul(2,3))

#only one liner function we can convert into lambda function not multiple line function