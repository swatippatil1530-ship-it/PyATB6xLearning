"""
\': Single quote (used to embed a single quote within a single-quoted string).
\": Double quote (used to embed a double quote within a double-quoted string).
\\: Backslash (used to represent a literal backslash character).
\n: Newline (inserts a line break).
\t: Horizontal tab (inserts a tab space).
\b: Backspace (moves the cursor back one position, potentially deleting a character).
\r: Carriage Return (moves the cursor to the beginning of the current line, overwriting previous content).
\a: ASCII Bell (produces an audible alert).
\v: Vertical Tab (inserts a vertical tab character).
\f: Form Feed (inserts a form feed character).
\xhh: Character with hexadecimal value hh (represents a character using its 8-bit hexadecimal value).
\ooo: Character with octal value ooo (represents a character using its 8-bit octal value).
\N{name}: Character named name in the Unicode database (e.g., \N{COPYRIGHT SIGN}).
\uxxxx: Character with 16-bit hexadecimal value xxxx (for Unicode characters).
\Uxxxxxxxx: Character with 32-bit hexadecimal value xxxxxxxx (for Unicode characters).
"""